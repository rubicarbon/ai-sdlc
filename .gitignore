# dev-only scratch space (init dry runs, adapter mocks, eval fixtures)
.dev/scratch/
.dev/debug*.txt

# Python bytecode (never part of the plugin package)
__pycache__/
*.py[cod]

# rendered PlantUML exports (docs/diagrams/*.puml is the source)
docs/diagrams/out/
